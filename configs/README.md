# Experiment configuration

StyleSeek exposes runtime and experiment settings through explicit command-line arguments. This directory is reserved for reusable, version-controlled configuration files as the experiment suite expands. Shared path defaults are centralized in `src/styleseek/paths.py`, while run-specific hyperparameters are recorded automatically in training histories and Ultralytics run metadata.
