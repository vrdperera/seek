# Experiment reports

- `metrics/`: machine-readable evaluation and latency JSON files.
- `figures/`: report-ready plots, confusion matrices, and qualitative retrieval examples.

Reports describe experiments; deployable weights belong under `artifacts/checkpoints/`.
